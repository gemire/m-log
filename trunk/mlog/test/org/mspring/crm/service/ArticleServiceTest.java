/**
 * 
 */
package org.mspring.crm.service;

import static org.junit.Assert.assertNotNull;

import java.util.List;

import org.junit.Before;
import org.junit.Test;
import org.mspring.crm.SpringContext;
import org.mspring.mlog.entity.Article;
import org.mspring.mlog.service.ArticleService;

/**
 * @author Gao Youbo
 * @since Mar 2, 2012
 */
public class ArticleServiceTest {
    @Before
    public void setUp() throws Exception {
    }

    @Test
    public void testGenerateTreeItem() {
        // ItemService itemService =
        // SpringContext.getContext().getBean(ItemService.class);
        // List<Item> items = itemService.generateTreeItem(1);
        // assertNotNull(items);
        // ArticleService articleService =
        // SpringContext.getContext().getBean(ArticleService.class);
        // List<Article> articles = articleService.findAllArticle();
        // assertNotNull(articles);
    }
}
